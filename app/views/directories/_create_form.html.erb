|--[<%= form_with url: directories_path, scope: :directory do |form| %>
  <%= form.text_field :name, value: nil, placeholder: 'Enter name' %>
  <%= form.hidden_field :parent_id, value: parent_id %>
<% end %>]
